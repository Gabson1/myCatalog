import { Router } from 'express';
import * as passport from 'passport'
// TODO: Add imports for public & private routes

const ServerRouter = Router();

// TODO: Add logic for public & private routes... Auth is handled by the route files themselves


export default ServerRouter;