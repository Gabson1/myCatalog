import { Router } from 'express';
// TODO: Import index.ts file from auths && index.ts file from routes

const ServerRouter = Router();

// TODO: Add logic for public & private routes... Auth is handled by the files in the dir ./auth and integrated into dir ./routes files


export default ServerRouter;